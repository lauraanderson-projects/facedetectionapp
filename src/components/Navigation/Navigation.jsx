import React from "react";

const Navigation = () => {
  return (
    <nav style={{ display: "flex", justifyContent: "flex-end" }}>
      <h2 className="f3 link dim white underline pa3 pointer">Sign Out</h2>
    </nav>
  );
};

export default Navigation;
